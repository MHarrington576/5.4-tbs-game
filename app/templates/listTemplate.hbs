{{#each character}}
  <div class="col-md-6">
    <img class="js-character" src="{{image}}" alt="{{name}}" data-character-name="{{name}}"/><br/>
    <h3>{{name}}</h3>
  </div>
{{/each}}

{{#each character}}
  <div class="col-md-6">
    <img class="js-heartless" src="{{image}}" alt="{{name}}" data-heartless-name="{{name}}"/><br/>
    <h3>{{name}}</h3>
  </div>
{{/each}}
